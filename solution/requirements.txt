# Runtime dependencies for the AMLS 2026 solution container.
# CPU-only -- no GPU wheels (see PDF Appendix A).
#
# torch is installed separately in the Dockerfile via the official CPU index URL
# because the standard PyPI wheel is GPU-enabled and exceeds the 4 GB image limit.
#
# pyarrow is pinned to 12.x -- versions >= 14 introduced a stricter parquet
# repetition-level histogram check that rejects the encoding used in this dataset.

numpy==1.26.4
pandas==2.2.2
pyarrow==16.1.0
pillow==10.4.0
scikit-learn==1.5.1
tqdm==4.66.5
